---
description: Verify Go code passes all style and best practice checks
allowed-tools: Glob, Grep, Read, Bash, Skill
---

Invoke the `skill-stack:golang` skill to verify Go code. Run the `/go:verify` workflow as defined in the skill — confirm that code passes all checks after changes have been applied.
